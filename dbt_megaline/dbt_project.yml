name: "dbt_megaline"
version: "1.0.0"

profile: "megaline"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
analysis-paths: ["analysis"]
target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  dbt_megaline:
    staging:
      +materialized: view
      +schema: silver
    gold:
      +materialized: table
      +schema: gold
